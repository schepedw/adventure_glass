$('.cart-subtotal').replaceWith("<%= j (render 'shopping_carts/cart_subtotal') %>");
$('.product-subtotal-<%=@updated_product.id%>').replaceWith("<%= j (render 'shopping_carts/product_subtotal', product: @updated_product) %>")
